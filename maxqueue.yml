includes:
  - configs/tsc/mplight.yml

model:
  name: "maxqueue"
  train_model: False
  test_model: True
  load_model: False
trainer:
  episodes: 1      ← only 1 episode needed!
  steps: 3600      ← full simulation
  test_steps: 3600